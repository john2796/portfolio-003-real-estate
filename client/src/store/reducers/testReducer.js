const initialState = {
  testing: "whatever"
};

export default function reducerName(state = initialState, action) {
  switch (action.type) {
    // case 'ACTION_TYPE':
    //   return code
    default:
      return state;
  }
}
