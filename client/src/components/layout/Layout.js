import React from "react";
import Header from "./HeaderContainer/Header";

const Layout = () => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
};

export default Layout;
