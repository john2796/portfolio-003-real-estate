import React from "react";
import Header from "./HeaderContainer/Header";

const Layout = () => {
  return (
    <React.Fragment>
      <Header />
      <p>search...</p>
    </React.Fragment>
  );
};

export default Layout;
